set-flag print-var-sorts "off"
set-flag print-qvar-sorts "off"

load "NN-sum"

datatype N := zero | (S N)
declare plus : [N N] -> N [+]
define [m n t] := [?m:N ?n:N ?t:N]

assert* Plus-zero-axiom := (n + zero = n)
assert* Plus-S-axiom := (n + S m = S (n + m))
